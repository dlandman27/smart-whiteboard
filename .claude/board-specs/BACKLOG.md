# Board Core Backlog

Maintained by the PM agent. Ranked by priority. Move items to `done` when merged.

## Pending

- **Store type safety** — remove remaining `any` casts in `whiteboard.ts` (e.g., `(widget as any).id`) and add proper typed interfaces
- **Theme var audit** — verify all ThemeVars keys are present and consistent across every preset
- **Layout validator** — utility to assert slot coordinates tile correctly (no overlaps, no out-of-bounds)

## In Progress

<!-- Items currently on a branch -->

## Done

<!-- Merged board-core changes -->
